import React from 'react';
import { Link } from 'react-router-dom';

const PokemonCard = ({ pokemon }) => {
	return (
		// pokemon card shorthand: pc
		<div className='pc'>
			<h2>This is a card for POKEMON</h2>
		</div>
	);
};

export default PokemonCard;
